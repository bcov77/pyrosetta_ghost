from setuptools import setup

__version__ = "1.0"

setup(
   name='pyrosetta_ghost',
   version='1.0',
   description='never let it die',
   author='bcov',
   author_email='bcov@uw.edu',
   packages=['pyrosetta_ghost'],  #same as name
   install_requires=[], #external packages as dependencies
)
